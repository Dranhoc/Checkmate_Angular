export enum Status {
  pending = 'pending',
  inProgress = 'inProgress',
  finished = 'finished',
}

export enum TournamentStatusCard {
  pending = 'pending',
  inProgress = 'in-progress',
  finished = 'finished',
  complete = 'complete',
  registered = 'registered',
}
